import type { IndexedPair } from './common';

/**
 * Key-value pair with numeric key (typically array index)
 * This is an alias of IndexedPair<string> for semantic clarity
 */
export type KeyValue = IndexedPair<string>;
